%% Model Estimation 1-D Case

% Parametric Estimation - Gaussian
[mu_a, sigma_a] = Gaussian1D(a);
[mu_b, sigma_b] = Gaussian1D(b);

x = 0:0.01:(max(a(1, :)) + 1);

figure;
plot(x, normpdf(x, mu_a, sigma_a));
plot(x, normpdf